<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_settings"
        android:title="@string/action_settings"
        android:icon="@android:drawable/ic_dialog_info"
        app:showAsAction="always"/>
    <item
        android:id="@+id/menuAbout"
        android:title="@string/aboutText"
        android:icon="@android:drawable/ic_menu_info_details"
        app:showAsAction="never"/>
    <item
        android:id="@+id/menuBuscar"
        android:title="@string/Buscar"
        android:icon="@android:drawable/ic_menu_search"
        app:showAsAction="never">
        <menu>
            <group android:checkableBehavior="single">
                <item android:id="@+id/search_by_name" android:title="@string/searchByName" android:checked="true"/>
                <item android:id="@+id/search_by_birth_date" android:title="@string/searchByBirthDate"/>
                <item android:id="@+id/search_by_last_name" android:title="@string/searchByLastName"/>
            </group>
        </menu>
    </item>
</menu>